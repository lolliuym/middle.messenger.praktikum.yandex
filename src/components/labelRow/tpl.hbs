 <div class="profile__row">
      <label class="{{cls_label}}">{{value}}
        <input
          class="{{cls_input}}"
          name="{{name}}"
          value="{{value_input}}"
          type="{{type}}"
          readonly
        />
       </label>
      </div>
